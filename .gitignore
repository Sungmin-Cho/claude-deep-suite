# Most of docs/ is internal working notes (plans, backlogs, analysis).
# Only the carve-outs below ship publicly: policy contract, auto-generated
# reference docs, and migration guides. Anything new in docs/ stays
# local-only by default.
docs/*
docs/superpowers/
!docs/memory-hierarchy.md
!docs/source-pinning.md
!docs/capability-matrix.md
!docs/artifact-io-graph.md
!docs/envelope-migration.md
node_modules/
.deep-suite-cache/
# Local Claude Code state (hook caches, sensor caches, session state) — runtime only, never committed.
.claude/
# Local /deep-review output (reports, responses, runtime config) — meant for audit, not public.
.deep-review/
