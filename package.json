{
  "name": "claude-deep-suite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deep Suite marketplace + sidecar metadata + validators",
  "packageManager": "npm@11.12.1",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "validate": "node scripts/validate-suite-extensions.js"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
