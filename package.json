{
  "name": "claude-deep-suite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deep Suite marketplace + sidecar metadata + validators",
  "packageManager": "npm@11.12.1",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-suite-extensions.js",
    "validate-artifact": "node scripts/validate-artifact.js",
    "validate-artifact-fixtures": "node scripts/validate-artifact-fixtures.js",
    "docs:check": "node scripts/generate-reference-sections.js --check",
    "docs:write": "node scripts/generate-reference-sections.js --write",
    "docs:sync": "node scripts/check-readme-plugin-table.js && node scripts/check-claude-md-paths.js && node scripts/check-guide-version.js && node scripts/check-semver-sha-sync.js && node scripts/check-pinned-plugin-paths.js && node scripts/check-memory-hierarchy.js",
    "preflight": "npm run validate && npm run docs:check && npm run docs:sync && npm run validate-artifact-fixtures && npm test",
    "release:bump": "node scripts/release-bump.js",
    "prepare": "node scripts/install-hooks.js"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
